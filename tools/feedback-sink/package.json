{
  "name": "feedback-sink",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker: reader feedback sink + passkey accounts + read-state sync",
  "scripts": {
    "test": "node test/smoke.mjs"
  },
  "dependencies": {
    "@simplewebauthn/server": "^13.1.1"
  }
}
