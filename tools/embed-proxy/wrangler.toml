name = "embed-proxy"
main = "src/worker.js"
compatibility_date = "2025-05-01"

# Workers AI binding — gives the worker env.AI.run(...) for embeddings.
[ai]
binding = "AI"

[observability]
enabled = true
