name = "og-proxy"
main = "src/worker.js"
compatibility_date = "2025-05-01"

# Observability: free, low-volume request logs viewable in `wrangler tail` or
# the Cloudflare dashboard. No paid plan required.
[observability]
enabled = true
